PORT=8080
DATABASE_URL=postgresql://postgres:password@localhost:5432/dhrms
JWT_SECRET=replace-with-existing-jwt-secret
JWT_EXPIRATION=86400000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# Supabase Storage: keep the bucket private; the secret key is server-side only.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=your-supabase-secret-key
SUPABASE_STORAGE_BUCKET=medical-records
IMAGE_MAX_SIZE_MB=10
PDF_MAX_SIZE_MB=20
